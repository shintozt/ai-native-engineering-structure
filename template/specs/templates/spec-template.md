# Spec：需求名称

> 本档是 Track 的"做什么"。proposal 必须先有 `approved-by` 才允许编辑本档。spec 没有 `approved-by` 之前禁止编辑 `design.md` / `tasks.md`。

## 定稿戳

- approved-by:
- approved-at:

## 用户故事 / 业务场景

## 业务不变式速览

> 必填段。每条必须写"条件 -> 必然行为"，并附一组反例。
> 高风险模块（去重、合流、状态机、时间窗、重采样、计费、权限、资金、库存等）至少给 3 条；其它至少给 1 条。

| INV-ID | 不变式（条件 -> 必然行为） | 反例 / 边界值 |
| --- | --- | --- |
| INV-001 |  |  |

## 功能行为

## 业务规则

> 每条业务规则必须命中至少 1 条 INV-ID；"边界值清单 / 反例"非空。

| 规则 ID | 命中 INV | 条件 | 系统行为 | 不变式 | 边界值清单 / 反例 | 异常处理 |
| --- | --- | --- | --- | --- | --- | --- |
| RULE-001 | INV-001 |  |  |  |  |  |

## 约束

### 必须遵守

- 

### 禁止

- 

## 数据与接口契约

### 输入

| 输入项 | 类型 / 来源 | 必填 | 约束 | 示例 |
| --- | --- | --- | --- | --- |

### 输出

| 输出项 | 类型 / 去向 | 语义 | 约束 | 示例 |
| --- | --- | --- | --- | --- |

### 错误码

| 错误码 / 错误类型 | 触发条件 | 系统行为 | 对调用方影响 | 是否可重试 |
| --- | --- | --- | --- | --- |

### 幂等性

> 本节用于提醒 AI 评估重复执行、状态推进和重试风险；只有涉及消息、缓存、状态机、水位、重试、队列、外部写入等场景时，才需要展开完整幂等设计。

| 场景 | 触发条件 | 系统行为 | 与缓存 / 队列 / 存储的关系 |
| --- | --- | --- | --- |
| （按需填写） |  |  |  |

- 幂等键：
- 重放、补偿或回滚策略：

## 边界条件

- 时间边界：
- 数据边界：
- 缓存边界：
- 并发边界：
- 异常输入：

## 非目标

## 验收标准

- [ ] 业务验收：
- [ ] 测试验收：
- [ ] 性能验收：
- [ ] 可观测性验收：

## 待确认问题

| 问题 | 当前假设 | 风险 | 确认人 | 阻塞级别 |
| --- | --- | --- | --- | --- |
|  |  |  |  | Blocking / Non-blocking |

## 追溯

- 来源需求：
- 关联设计：
- 关联任务：

## 知识引用

| 知识 ID | 标题 | 使用位置 |
| --- | --- | --- |

## 定稿要求

- [ ] 业务不变式速览段非占位（每条 INV-XXX 都有反例 / 边界值）。
- [ ] 业务规则表每行都命中至少 1 条 INV-ID，且"边界值清单 / 反例"非空。
- [ ] 幂等性已按需求风险填写；不涉及时说明"不适用"。
- [ ] Blocking 问题全部关闭。
- [ ] 验收标准每条都可测试。
- 全部勾选后，在"定稿戳"补 `approved-by` / `approved-at`，再走下一档。
