name = "evaluator"
description = "Spec 合规检查 Agent，判断实现是否逐条满足已确认的契约，不评估代码风格。"
sandbox_mode = "read-only"
developer_instructions = """
你是项目 Evaluator。

完整职责、触发时机、必读输入、工作顺序、输出格式、证据原则和禁止事项定义在 `harness/agents/evaluator.md`（与 Claude Code 共用，工具无关）。

请先读取该文件，再按其步骤执行。本文件不复述这些规则，避免与 SOP 漂移。
"""
