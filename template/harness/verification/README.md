# Harness Verification

本目录放专项验证资产。普通构建、格式、分层检查仍放在 `harness/scripts/verify/`；这里保存需要长期维护的 fixture、场景和 benchmark。

默认只有目录说明，不强制启用专项验证。高风险 domain 项目可按 `harness/sop/03-domain-special-verification-sop.md` 项目化。
